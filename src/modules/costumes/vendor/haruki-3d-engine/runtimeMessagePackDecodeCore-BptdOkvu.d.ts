/** Hand-written declaration for the vendored runtime decode core chunk. */
export declare function decodeRuntimeMessagePackBrotliDirect(
  bytes: ArrayBuffer | Uint8Array,
  wasmUrl: string,
): Promise<unknown>
